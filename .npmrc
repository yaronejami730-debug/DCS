# Expo / Metro do not follow pnpm's symlinked store reliably.
# Hoisted node-linker is the layout Expo officially supports in monorepos.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
